# EC2-Quiz-
Review your lecture notes from the previous four topics and select the single best answer for each of the following 10 questions.

1. Why is Amazon EC2 categorized as Infrastructure as a Service (IaaS)?

A) It completely manages application code deployment automatically.

B) It provides raw, configurable fundamental infrastructure components like virtual machines, storage, and load balancers.

C) It delivers ready-to-use software applications over the internet without server setup.

D) It is exclusively used for automated database administrative maintenance.

2. Which of the following statements is true regarding EC2 User Data scripts?

A) They execute every time the instance is manually restarted or rebooted.

B) They can be executed by any standard user account with read permissions.

C) They run only once during the first boot of the instance and are executed by the root user.

D) They are primarily used to monitor real-time CPU usage metrics.

3. In the EC2 instance naming format "t2.micro" or "m5.2xlarge", what does the numeric digit (e.g., 2 or 5) represent?

A) The number of virtual CPUs assigned to the server instance.

B) The generation of the instance released by AWS.

C) The maximum number of network security groups attached.

D) The amount of storage disks allocated.

4. Which EC2 instance category is most appropriate for high-performance computing (HPC), machine learning model training, and game servers?

A) Storage Optimized
B) Memory Optimized
C) Compute Optimized
D) General Purpose

5. If you are deploying an in-memory database or a real-time data processing application that handles massive data sets locally, which instance type should you select?

A) General Purpose
B) Compute Optimized
C) Storage Optimized
D) Memory Optimized

6. Security groups act as virtual firewalls at the network layer.

Which type of rules can they contain?

A) Both explicit allow and explicit deny rules.
B) Only explicit deny rules.
C) Only allow rules.
D) Explicit redirection rules only.
7. What is the default traffic configuration for a newly created AWS Security Group?

A) All inbound traffic is blocked, and all outbound traffic is allowed.
B) All inbound traffic is allowed, and all outbound traffic is blocked.
C) All inbound and outbound traffic are completely blocked.
D) All inbound and outbound traffic are wide open to any destination network.
8. To allow secure command-line terminal access to a Linux EC2 instance, which port must be open in your security group rules?

A) Port 80 (HTTP)
B) Port 22 (SSH)
C) Port 443 (HTTPS)
D) Port 3389 (RDP)

9. Which administrative task does Amazon RDS handle automatically to minimize operational overhead for a development team?

A) Writing complex SQL queries for application features.
B) Designing customized database tables and relationships.
C) Applying operating system security patches and running automated backups.
D) Choosing the frontend user authentication protocols.

10. How does Auto Scaling operate within Amazon RDS compared to Amazon EC2?

A) RDS automatically adds more database server instances to handle traffic.
B) RDS auto-scales processing units (CPU) and volatile memory (RAM) dynamically.
C) RDS auto-scaling applies exclusively to dynamic storage capacity growth, unlike EC2 which handles compute auto-scaling.
D) RDS does not support any automatic scaling mechanisms.
 
